import React from "react";

import { Chart as ChartJS } from "chart.js/auto";

export default function DoughnutChart(chartName) {
  // ChartJS.register(ArcElement, Tooltip, Legend);

  return <chartName data={chartData} />;
}
